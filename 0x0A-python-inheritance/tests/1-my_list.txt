=========================

The 1-my_list module

========================

Usage: MyList

========================

""" MyList inherits the list class """

Module inclusion:
    >>> MyList = __import__('1-my_list').MyList

Success Cases:

List with positive integers:
    >>> my_list = MyList()
    >>> my_list.append(1)
    >>> my_list.append(3)
    >>> my_list.append(4)
    >>> my_list.append(2)
    >>> print(my_list)
    [1, 3, 4, 2]
    >>> my_list.print_sorted()
    [1, 2, 3, 4]
    >>> print(my_list)
    [1, 2, 3, 4]

List with Negative integers:
    >>> my_
